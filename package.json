{
  "name": "career-roadmap-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "autocannon": "^8.0.0",
    "exceljs": "^4.4.0"
  }
}
